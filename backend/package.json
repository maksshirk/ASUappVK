{
  "name": "faculty-backend",
  "version": "1.0.0",
  "description": "Backend-сервер для VK Mini App факультета №9. Обработка пользователей, профилей и данных из MongoDB.",
  "main": "server.js",
  "type": "commonjs",
  "author": "Максим",
  "license": "MIT",

  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "eslint . --ext js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^4.19.2",
    "mongoose": "^8.3.0"
  },

  "devDependencies": {
    "nodemon": "^3.1.14"
  },

  "engines": {
    "node": ">=18.0.0"
  }
}